### Read Key Trusted Application

Read a `<cli_id, cli_key>` pair in Secure Storage, given the `cli_id` parameter. 

The TA takes one single parameter through the command line:
    + `cli_id`: the client ID in the MQTTZ format.
